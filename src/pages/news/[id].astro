---
import { getCollection, render, type CollectionEntry } from 'astro:content';
import ChevronRightIcon from 'lucide-astro/ChevronRight';
import ChevronLeftIcon from 'lucide-astro/ChevronLeft';

import ProseLayout from '@layouts/ProseLayout.astro';
import CompanyLogo from '@components/CompanyLogo.astro';
import { paths } from '@data/paths';
import { formatDate } from '@utils/date';
import { filterDrafts, getCategoryLabel } from '@utils/content';
import { twMerge } from 'tailwind-merge';
import { categories } from 'src/content.config';

export async function getStaticPaths() {
  const posts = await getCollection('news', filterDrafts);
  const sortedPosts = posts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

  return sortedPosts.map((post, index) => ({
    params: { id: post.id },
    props: {
      post,
      previousPost: sortedPosts[index - 1] || null,
      nextPost: sortedPosts[index + 1] || null
    }
  }));
}

interface Props {
  post: CollectionEntry<'news'>;
  previousPost: CollectionEntry<'news'> | null;
  nextPost: CollectionEntry<'news'> | null;
}

const { post, previousPost, nextPost } = Astro.props;
const { Content } = await render(post);
---

<ProseLayout title={post.data.title} description={post.data.description} image={post.data.cover}>
  <article>
    <header class="not-prose text-center">
      <h1 class="text-3xl font-semibold text-neutral-950 md:text-4xl dark:text-neutral-50">
        {post.data.title}
      </h1>
      <div class="mt-6 flex flex-wrap justify-center gap-1.5 text-sm text-neutral-500">
        {
          post.data.author && (
            <>
              <span>{post.data.author}</span>
              <span>&middot;</span>
            </>
          )
        }
        <time datetime={post.data.date.toISOString()}>{formatDate(post.data.date)}</time>
        <span>&middot;</span>
        <a
          href={`${paths.news.href}/${post.data.category}`}
          class="transition hover:text-neutral-800 active:text-neutral-800 dark:hover:text-neutral-200 dark:active:text-neutral-200"
        >
          {getCategoryLabel(post.data.category)}
        </a>
        <span>&middot;</span>
        <button
          id="copy-clipboard"
          class="w-16 cursor-pointer text-left transition hover:text-neutral-800 active:text-neutral-800 dark:hover:text-neutral-200 dark:active:text-neutral-200"
        >
          Copy link
        </button>
      </div>
    </header>
    {
      post.data.callout && post.data.category === 'case-example' && (
        <div class="not-prose aspect-wide mx-auto my-16 flex items-center rounded-md border border-neutral-200 bg-white px-4 md:aspect-3/1 dark:border-neutral-800 dark:bg-neutral-900">
          <div class="flex w-1/2 justify-center p-4">
            <div>
              <p
                class:list={twMerge(
                  post.data.callout.value.length < 6 ? 'text-4xl' : 'text-2xl',
                  'max-w-48 leading-none font-semibold text-neutral-950 dark:text-neutral-50'
                )}
              >
                {post.data.callout.value}
              </p>
              <p class="mt-2 text-sm leading-none text-neutral-500">{post.data.callout.label}</p>
            </div>
          </div>
          <div class="w-px shrink-0 self-stretch bg-neutral-200 dark:bg-neutral-800" />
          <div class="flex w-1/2 justify-center p-4">
            <CompanyLogo
              company={post.data.callout.company}
              class="h-7 w-auto shrink-0 text-neutral-950 dark:text-neutral-50"
            />
          </div>
        </div>
      )
    }
    <section class="my-10 md:my-12 lg:my-14 xl:my-16">
      <Content />
    </section>
  </article>
  <footer
    class="not-prose border-t border-neutral-200 pt-10 md:pt-12 lg:pt-14 xl:pt-16 dark:border-neutral-800"
  >
    <nav class="grid grid-cols-1 gap-8 sm:grid-cols-2">
      {
        previousPost && (
          <a
            href={`/news/${previousPost.id}`}
            class="group flex flex-col items-start gap-1 text-left"
          >
            <div class="flex items-center gap-0.5 text-neutral-500 transition group-hover:text-current group-active:text-current">
              <ChevronLeftIcon size={14} class="mt-px transition group-hover:-translate-x-0.5" />
              <span class="text-sm">Previous</span>
            </div>
            <p class="text-base font-semibold">{previousPost.data.title}</p>
          </a>
        )
      }
      {
        nextPost && (
          <a
            href={`/news/${nextPost.id}`}
            class="group flex flex-col items-end gap-1 text-right md:col-start-2"
          >
            <div class="flex items-center gap-0.5 text-neutral-500 transition group-hover:text-current group-active:text-current">
              <span class="text-sm">Next</span>
              <ChevronRightIcon size={14} class="mt-px transition group-hover:translate-x-0.5" />
            </div>
            <p class="text-base font-semibold">{nextPost.data.title}</p>
          </a>
        )
      }
    </nav>
  </footer>
</ProseLayout>

<script>
  const button = document.getElementById('copy-clipboard');

  button?.addEventListener('click', () => {
    try {
      navigator.clipboard.writeText(window.location.href);
      button.textContent = 'Copied!';
      setTimeout(() => {
        button.textContent = 'Copy link';
      }, 3000);
    } catch (e) {
      console.log(e);
    }
  });
</script>
