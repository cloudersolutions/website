---
import type { NavItem } from '@data/types';
import navigation from '@data/navigation.yml';
import site from '@data/site.yml';
import Logo from '@components/Logo.astro';

const { pathname } = Astro.url;
const navItems: NavItem[] = navigation;
---

<footer class="border-t border-neutral-200 py-12 dark:border-neutral-800">
  <div class="container grid grid-cols-2 gap-10 sm:grid-cols-3">
    <div class="col-span-2 sm:col-span-1">
      <Logo size={16} class="fill-neutral-600 dark:fill-neutral-400" />
      <p class="mt-4 text-sm text-neutral-500">&copy; {new Date().getFullYear()} {site.title}</p>
    </div>
    <div class="flex flex-col gap-3">
      <h4 class="text-sm font-semibold text-neutral-600 dark:text-neutral-400">Resources</h4>
      {
        navItems.map(({ title, href }) => (
          <a
            href={href}
            class="text-sm text-neutral-500 transition hover:text-neutral-900 dark:hover:text-neutral-200"
          >
            {title}
          </a>
        ))
      }
    </div>
    <div class="flex flex-col gap-3">
      <h4 class="text-sm font-semibold text-neutral-600 dark:text-neutral-400">Follow</h4>
      {
        site.twitter && (
          <a
            href={site.twitter}
            class="text-sm text-neutral-500 transition hover:text-neutral-900 dark:hover:text-neutral-200"
          >
            X (Twitter)
          </a>
        )
      }
      {
        site.linkedin && (
          <a
            href={site.linkedin}
            class="text-sm text-neutral-500 transition hover:text-neutral-900 dark:hover:text-neutral-200"
          >
            LinkedIn
          </a>
        )
      }
      {
        site.instagram && (
          <a
            href={site.instagram}
            class="text-sm text-neutral-500 transition hover:text-neutral-900 dark:hover:text-neutral-200"
          >
            Instagram
          </a>
        )
      }
      {
        site.youtube && (
          <a
            href={site.youtube}
            class="text-sm text-neutral-500 transition hover:text-neutral-900 dark:hover:text-neutral-200"
          >
            YouTube
          </a>
        )
      }
    </div>
  </div>
</footer>
